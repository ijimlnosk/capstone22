/*
package com.daelim.capstone22

import android.content.Context
import android.view.View
import android.widget.TextView
import androidx.recyclerview.widget.RecyclerView
import com.daelim.capstone22.data.ListData
import com.daelim.capstone22.data.ListResponse

class MyAdapter(private val context: Context) : RecyclerView.Adapter<MyAdapter.ViewHolder>(){
    inner class ViewHolder(view: View) : RecyclerView.ViewHolder(view){

        private val tvName = itemView.findViewById<TextView>(R.id.tvName)
        private val tvAmount = itemView.findViewById<TextView>(R.id.tvAmount)
        private val tvCategory = itemView.findViewById<TextView>(R.id.tvCategory)
        private val tvTransaction = itemView.findViewById<TextView>(R.id.tvTransaction)

        fun bind(item: ListData){
            */
/* tvBD.text = item.breakdown
             tvPay.text = item.pay
             tvCate.text = item.name*//*

        }


}*/
